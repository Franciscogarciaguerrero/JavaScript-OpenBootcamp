const alturaCm = 180   
const alturaM = 1.71
const peso = 85.5
const alturaMRedUp = Math.ceil(alturaM)
console.log(alturaMRedUp)
const pesoRed = Math.floor(peso)
console.log(pesoRed)
const maxValue = Number.MAX_VALUE + 1 === Number.MAX_VALUE
console.log(maxValue)